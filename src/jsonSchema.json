{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "title": {
      "type": "string",
      "title": "Title",
      "description": "Enter the title of the event."
    },
    "type": {
      "type": "string",
      "title": "Type",
      "description": "Select the type of the event.",
      "enum": ["generic", "holiday"],
      "enumNames": ["Generic", "Holiday"]
    },
    "startDate": {
      "type": "string",
      "format": "date",
      "title": "Start Date",
      "description": "Select the start date of the event."
    },
    "endDate": {
      "type": "string",
      "format": "date",
      "title": "End Date",
      "description": "Select the end date of the event."
    },
    "description": {
      "type": "string",
      "title": "Description",
      "description": "Enter a description of the event."
    }
  },
  "required": ["title", "startDate", "endDate"]
}
